check.count.description=Check if the number of modules is grater than 1000.
check.feedback.description=Check if there is a feedback in the circuit.
check.illegalconnection.description=Check if an incoming port is connected to another incoming port and vice versa.
check.multipleconnections.description=Check if there are multiple connections on one incoming port.
check.orphan.description=Check if there are modules which are not connected to other modules.
check.sink.description=Check if all modules are (in)directly connected to a lamp.
check.source.description=Check if all modules are (in)directly connected to an impulse generator.